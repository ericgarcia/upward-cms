extends ../layouts/default

block content
  .container
    .articles
      a(name='articles')
      .page-header 
        h1 Articles
        hr
      each article in data.articles
        //- .row.container
          //- .col-md-4
          //-   .img(style="background-image: url(#{caseStudy.image.url})")
          //- .col-md-8
        .article.row
          .col-md-2
            .title #{article.title}
          .col-md-2
            a(href="#{protocol}:#{article.content.url}") download file
